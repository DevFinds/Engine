name: shapesider
recipe: lemp
config:
  via: nginx
  config:
    vhosts: .lando/nginx.conf
  php: "8.1"
  webroot: public
  services:
    mariadb:
      type: mariadb:10.5
    phpmyadmin:
      type: phpmyadmin
    appserver:
      overrides:
        volumes:
          - ./storage:/app/public/storage
